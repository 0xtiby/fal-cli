{
  "name": "fal-cli",
  "version": "0.1.0",
  "type": "module",
  "description": "CLI tool for generating and editing images via the fal.ai API",
  "bin": {
    "fal-cli": "src/index.js"
  },
  "main": "src/index.js",
  "scripts": {
    "build": "echo 'No build step required'",
    "lint": "echo 'No lint configured yet'",
    "test": "node --test src/**/*.test.js"
  },
  "dependencies": {
    "@fal-ai/client": "^1.9.4",
    "@inquirer/prompts": "^8.3.2",
    "dotenv": "^17.3.1",
    "incur": "^0.3.4",
    "ora": "^9.3.0"
  }
}
